override_subgraph_url:
  products: http://localhost:4005
redact_subgraph_errors:
  all: false

supergraph:
  query_planning:
    cache:
      redis:
        urls: ["redis://localhost:6379",]

telemetry:
  exporters:
    logging:
      stdout:
        format: text
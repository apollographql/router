telemetry:
  tracing:
    trace_config:
      service_name: router
    jaeger:
      batch_processor:
        scheduled_delay: 100ms
      agent:
        endpoint: default
override_subgraph_url:
  products: http://localhost:4005

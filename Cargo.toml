[workspace]
default-members = ["apollo-router", "apollo-router-core"]
members = [
    "apollo-spaceport",
    "apollo-router",
    "apollo-router-core",
    "apollo-router-benchmarks",
    "fuzz",
    "uplink",
    "xtask",
    "examples/*",
]

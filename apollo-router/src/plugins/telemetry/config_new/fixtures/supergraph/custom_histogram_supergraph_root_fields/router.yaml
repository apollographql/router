telemetry:
  instrumentation:
    instruments:
      supergraph:
        'query.root_fields':
          description: 'The root_fields of the query'
          value:
            query: root_fields
          unit: unit
          type: histogram

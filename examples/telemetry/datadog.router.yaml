# Enable diagnostics for local debugging (http://127.0.0.1:8089/diagnostics/)
experimental_diagnostics:
  enabled: true

telemetry:
  exporters:
    tracing:
      common:
        service_name: router
      datadog:
        enabled: true
        endpoint: default

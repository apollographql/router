extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.12", import: ["@key"])
  @link(url: "https://specs.apollo.dev/connect/v0.3", import: ["@connect", "@source"])
  @source(name: "svc_43", http: { baseURL: "https://svc43.example.com" })

type Query {
  svc43_item0(id: ID!): Svc43Item0
    @connect(
      source: "svc_43"
      http: { GET: "/items0/{$args.id}" }
      selection: "id name value"
    )

  svc43_item1(id: ID!): Svc43Item1
    @connect(
      source: "svc_43"
      http: { GET: "/items1/{$args.id}" }
      selection: "id name value"
    )

  svc43_item2(id: ID!): Svc43Item2
    @connect(
      source: "svc_43"
      http: { GET: "/items2/{$args.id}" }
      selection: "id name value"
    )

  svc43_item3(id: ID!): Svc43Item3
    @connect(
      source: "svc_43"
      http: { GET: "/items3/{$args.id}" }
      selection: "id name value"
    )

  svc43_item4(id: ID!): Svc43Item4
    @connect(
      source: "svc_43"
      http: { GET: "/items4/{$args.id}" }
      selection: "id name value"
    )

  svc43_item5(id: ID!): Svc43Item5
    @connect(
      source: "svc_43"
      http: { GET: "/items5/{$args.id}" }
      selection: "id name value"
    )

  svc43_item6(id: ID!): Svc43Item6
    @connect(
      source: "svc_43"
      http: { GET: "/items6/{$args.id}" }
      selection: "id name value"
    )

  svc43_item7(id: ID!): Svc43Item7
    @connect(
      source: "svc_43"
      http: { GET: "/items7/{$args.id}" }
      selection: "id name value"
    )

  svc43_shared0(id: ID!): Shared0
    @connect(
      source: "svc_43"
      http: { GET: "/shared0/{$args.id}" }
      entity: true
      selection: "id s43_a s43_b"
    )

  svc43_shared1(id: ID!): Shared1
    @connect(
      source: "svc_43"
      http: { GET: "/shared1/{$args.id}" }
      entity: true
      selection: "id s43_a s43_b"
    )

  svc43_shared2(id: ID!): Shared2
    @connect(
      source: "svc_43"
      http: { GET: "/shared2/{$args.id}" }
      entity: true
      selection: "id s43_a s43_b"
    )

  svc43_shared3(id: ID!): Shared3
    @connect(
      source: "svc_43"
      http: { GET: "/shared3/{$args.id}" }
      entity: true
      selection: "id s43_a s43_b"
    )
}

type Svc43Item0 {
  id: ID!
  name: String
  value: Int
}

type Svc43Item1 {
  id: ID!
  name: String
  value: Int
}

type Svc43Item2 {
  id: ID!
  name: String
  value: Int
}

type Svc43Item3 {
  id: ID!
  name: String
  value: Int
}

type Svc43Item4 {
  id: ID!
  name: String
  value: Int
}

type Svc43Item5 {
  id: ID!
  name: String
  value: Int
}

type Svc43Item6 {
  id: ID!
  name: String
  value: Int
}

type Svc43Item7 {
  id: ID!
  name: String
  value: Int
}

type Shared0 @key(fields: "id") {
  id: ID!
  s43_a: String
  s43_b: String
}

type Shared1 @key(fields: "id") {
  id: ID!
  s43_a: String
  s43_b: String
}

type Shared2 @key(fields: "id") {
  id: ID!
  s43_a: String
  s43_b: String
}

type Shared3 @key(fields: "id") {
  id: ID!
  s43_a: String
  s43_b: String
}

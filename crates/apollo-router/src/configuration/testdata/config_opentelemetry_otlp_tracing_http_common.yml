server:
  listen: 1.2.3.4:5
subgraphs:
  example:
    routing_url: foo
opentelemetry:
  otlp:
    tracing:
      exporter:
        http:
          endpoint: http://example.org
          protocol: Grpc
          timeout: 42

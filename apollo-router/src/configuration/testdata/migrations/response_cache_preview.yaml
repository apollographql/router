preview_response_cache:
  enabled: true
  subgraph:
    all:
      redis:
        urls: ["redis://test"]
        required_to_start: true
      enabled: true
      invalidation:
        enabled: true
        shared_key: "invalidate"
    subgraphs:
      accounts:
        enabled: false
      products:
        ttl: 120s
        invalidation:
          enabled: true
          shared_key: "invalidate"

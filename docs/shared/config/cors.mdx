### `cors`

```yaml title="cors"
cors:
  allow_any_origin: false
  allow_credentials: false
  allow_headers: []
  expose_headers: null
  max_age: null
  methods:
  - POST
  - PUT
  policies:
  - origins:
    - https://studio.apollographql.com
    - https://myapp.com
    allow_credentials: false
    allow_headers: []
    expose_headers: []
    # methods not specified - uses global defaults [POST, PUT]
  - origins:
    - https://restricted.com
    methods: []  # Explicitly no methods allowed
  - origins:
    - https://api.example.com
    match_origins:
    - "^https://.*\\.example\\.com$"
    allow_headers:
    - content-type
    - authorization
    methods:
    - GET
    - POST
    # Specific methods override global defaults
```

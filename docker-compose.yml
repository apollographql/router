version: "3.9"
services:
  services:
    build:
      context: ./dockerfiles
      dockerfile: Dockerfile.federation-demo
    ports:
      - 4100:4000
      - 4001:4001
      - 4002:4002
      - 4003:4003
      - 4004:4004
    command: ./node_modules/.bin/concurrently 'npm:start-services' 'sleep 2; npm run start-gateway'

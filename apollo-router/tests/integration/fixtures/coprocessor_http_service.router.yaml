# Coprocessor with only http_service stage (router front HTTP hook).
# Used by test_coprocessor_http_service_stage to verify the hook runs in the real binary.
coprocessor:
  url: "<replace>"
  http_service:
    request:
      body: true
      headers: true
    response:
      body: true
      headers: true
      status_code: true

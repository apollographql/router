extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.12", import: ["@key"])
  @link(url: "https://specs.apollo.dev/connect/v0.3", import: ["@connect", "@source"])
  @source(name: "svc_55", http: { baseURL: "https://svc55.example.com" })

type Query {
  svc55_item0(id: ID!): Svc55Item0
    @connect(
      source: "svc_55"
      http: { GET: "/items0/{$args.id}" }
      selection: "id name value"
    )

  svc55_item1(id: ID!): Svc55Item1
    @connect(
      source: "svc_55"
      http: { GET: "/items1/{$args.id}" }
      selection: "id name value"
    )

  svc55_item2(id: ID!): Svc55Item2
    @connect(
      source: "svc_55"
      http: { GET: "/items2/{$args.id}" }
      selection: "id name value"
    )

  svc55_item3(id: ID!): Svc55Item3
    @connect(
      source: "svc_55"
      http: { GET: "/items3/{$args.id}" }
      selection: "id name value"
    )

  svc55_item4(id: ID!): Svc55Item4
    @connect(
      source: "svc_55"
      http: { GET: "/items4/{$args.id}" }
      selection: "id name value"
    )

  svc55_item5(id: ID!): Svc55Item5
    @connect(
      source: "svc_55"
      http: { GET: "/items5/{$args.id}" }
      selection: "id name value"
    )

  svc55_item6(id: ID!): Svc55Item6
    @connect(
      source: "svc_55"
      http: { GET: "/items6/{$args.id}" }
      selection: "id name value"
    )

  svc55_item7(id: ID!): Svc55Item7
    @connect(
      source: "svc_55"
      http: { GET: "/items7/{$args.id}" }
      selection: "id name value"
    )

  svc55_shared0(id: ID!): Shared0
    @connect(
      source: "svc_55"
      http: { GET: "/shared0/{$args.id}" }
      entity: true
      selection: "id s55_a s55_b"
    )

  svc55_shared1(id: ID!): Shared1
    @connect(
      source: "svc_55"
      http: { GET: "/shared1/{$args.id}" }
      entity: true
      selection: "id s55_a s55_b"
    )

  svc55_shared2(id: ID!): Shared2
    @connect(
      source: "svc_55"
      http: { GET: "/shared2/{$args.id}" }
      entity: true
      selection: "id s55_a s55_b"
    )

  svc55_shared3(id: ID!): Shared3
    @connect(
      source: "svc_55"
      http: { GET: "/shared3/{$args.id}" }
      entity: true
      selection: "id s55_a s55_b"
    )
}

type Svc55Item0 {
  id: ID!
  name: String
  value: Int
}

type Svc55Item1 {
  id: ID!
  name: String
  value: Int
}

type Svc55Item2 {
  id: ID!
  name: String
  value: Int
}

type Svc55Item3 {
  id: ID!
  name: String
  value: Int
}

type Svc55Item4 {
  id: ID!
  name: String
  value: Int
}

type Svc55Item5 {
  id: ID!
  name: String
  value: Int
}

type Svc55Item6 {
  id: ID!
  name: String
  value: Int
}

type Svc55Item7 {
  id: ID!
  name: String
  value: Int
}

type Shared0 @key(fields: "id") {
  id: ID!
  s55_a: String
  s55_b: String
}

type Shared1 @key(fields: "id") {
  id: ID!
  s55_a: String
  s55_b: String
}

type Shared2 @key(fields: "id") {
  id: ID!
  s55_a: String
  s55_b: String
}

type Shared3 @key(fields: "id") {
  id: ID!
  s55_a: String
  s55_b: String
}

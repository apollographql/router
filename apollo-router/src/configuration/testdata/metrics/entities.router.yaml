preview_entity_cache:
  enabled: false
  redis:
    urls: [ "redis://localhost:6379" ]
    timeout: 5ms
    ttl: 60s
  invalidation:
    listen: 127.0.0.1:4000
    path: /invalidation
  subgraph:
    all:
      enabled: true
    subgraphs:
      accounts:
        enabled: false
      products:
        ttl: 120s
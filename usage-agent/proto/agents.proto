syntax = "proto3";

import "proto/reports.proto";

package Agent;

service Reporter {
    rpc SendReport(Report.Report) returns (ReportResponse) {}
}

message ReportResponse {
    string message = 1;
}

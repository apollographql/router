telemetry:
  apollo:
    subgraph_metrics: true
  instrumentation:
    instruments:
      subgraph:
        http.client.request.duration: false
        http.client.request.body.size: false
        http.client.response.body.size: false
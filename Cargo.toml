[workspace]
default-members = ["apollo-router", "apollo-router-core"]
members = [
    "apollo-spaceport",
    "apollo-router",
    "apollo-router-core",
    "apollo-router-benchmarks",
    "examples/async-auth",
    "examples/cookies-to-headers",
    "examples/embedded",
    "examples/context",
    "examples/forbid-anonymous-operations",
    "examples/hello-world",
    "examples/status-code-propagation",
    "examples/jwt-auth",
    "fuzz",
    "uplink",
    "xtask",
]

# Temporarily comment out to see if Circle-CI build env in linux is broken by this
# this makes build scripts and proc macros faster to compile
# [profile.dev.build-override]
# debug = false
# strip = "debuginfo"
# incremental = false

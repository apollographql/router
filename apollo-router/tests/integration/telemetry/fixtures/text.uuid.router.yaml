telemetry:
  instrumentation:
    spans:
      mode: spec_compliant
    events:
      router:
        # Standard events
        request: info
        response: info
        error: info
  exporters:
    logging:
      stdout:
        format:
          text:
            display_trace_id: uuid
            display_span_id: true

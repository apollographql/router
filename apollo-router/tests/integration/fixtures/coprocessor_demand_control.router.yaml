# This coprocessor url will be updated to a test-scoped mock server
coprocessor:
  url: "<replace>"
  execution:
    request:
      context: all
  supergraph:
    response:
      context: all

demand_control:
  enabled: true
  mode: measure
  strategy:
    static_estimated:
      list_size: 10
      max: 1000

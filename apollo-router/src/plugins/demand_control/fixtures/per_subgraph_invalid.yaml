demand_control:
  enabled: true
  mode: enforce
  strategy:
    static_estimated:
      list_size: 1
      max: 10
      subgraph:
        all:
          list_size: 3
        subgraphs:
          products:
            max: -1

```mermaid
flowchart RL
    subgraph client["Client"]
    end

    subgraph router["Router"]
        direction LR
        httpLayer("HTTP <br/> layer")
        routerService("Router <br/> Service")
        supergraphService("Supergraph <br/> Service")
        executionService("Execution <br/> Service")
        subgraphService("Subgraph <br/> Service")
        httpLayer -->|request| routerService -->|request| supergraphService -->|request| executionService -->|request| subgraphService
        subgraphService -->|response| executionService -->|response| supergraphService -->|response| routerService -->|response| httpLayer

    end

    subgraph infra["Your infrastructure"]
      direction TB
      api1("subgraph A");
      api2("subgraph B");
      api3("subgraph C");
      api1 --- api2 --- api3 

    end

client -->|request| httpLayer
subgraphService -->|request| infra
infra -->|response| subgraphService
httpLayer -->|response| client
```

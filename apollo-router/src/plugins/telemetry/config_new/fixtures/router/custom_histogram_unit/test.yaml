description: Custom histogram unit
events:
  - - router_request:
        uri: "/hello"
        method: GET
        body: |
          hello
    - router_response:
        body: |
          hello
        status: 200
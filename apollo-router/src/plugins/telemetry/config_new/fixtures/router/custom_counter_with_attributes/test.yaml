description: Custom counter with attributes
events:
  - - router_request:
        uri: "/hello"
        method: GET
        headers:
          custom_header: custom_value
        body: |
          hello
    - router_response:
        body: |
          hello
        status: 200
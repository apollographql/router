preview_file_uploads:
  enabled: true
  protocols:
    multipart:
      enabled: true
      mode: stream
      limits:
        max_file_size: 15gb
        max_files: 10
        operation_body_timeout: 1s
include_subgraph_errors:
  all: true

traffic_shaping:
  router:
    timeout: 15s

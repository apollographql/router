include_subgraph_errors:
  all: true

preview_connectors:
  debug_extensions: true
  subgraphs:
    connectors:
      sources:
        jsonPlaceholder:
          override_url: http://localhost:4007

telemetry:
  exporters:
    logging:
      stdout:
        format: text
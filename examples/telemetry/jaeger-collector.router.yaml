telemetry:
  tracing:
    trace_config:
      service_name: router
    jaeger:
      enabled: true
      collector:
        endpoint: "https://example.com"
        username: "username"
        password: "password"


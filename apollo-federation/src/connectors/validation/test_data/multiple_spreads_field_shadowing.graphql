extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.13")
  @link(
    url: "https://specs.apollo.dev/connect/v0.4"
    import: ["@connect", "@source"]
  )
  @source(
    name: "products-api"
    http: { baseURL: "http://localhost:3001" }
  )

interface Product {
  upc: ID!
}

type Book implements Product {
  upc: ID!
  title: String
  author: Person
  hi: String
}

type Film implements Product {
  upc: ID!
  title: String
  director: Person
}

type Person {
  id: ID!
  name: String
}

# First spread sets hi: $('Ben1'), second spread sets hi: $('Ben2').
# The second spread shadows the first for Book shapes.
type Query {
  products: [Product] @connect(
    source: "products-api"
    http: { GET: "/products" }
    selection: """
      upc
      ... category->match(
        ['book', $ {
          __typename: $('Book')
          title
          author { id name }
          hi: $('Ben1')
        }],
        ['movie', $ {
          __typename: $('Film')
          title
          director { id name }
        }],
        [@, null]
      )
      ... category->match(
        ['book', $ {
          __typename: $('Book')
          hi: $('Ben2')
        }]
      )
    """
  )
}

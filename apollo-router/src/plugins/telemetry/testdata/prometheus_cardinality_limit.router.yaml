telemetry:
  exporters:
    metrics:
      common:
        service_name: apollo-router
        cardinality_limit: 5000
        views:
          - name: apollo.test.histo
            cardinality_limit: 2
            allowed_attribute_keys:
              - otel_scope_name
              - k
      prometheus:
        enabled: true

name: CI
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Missing Changelog Reminder
        uses: mskelton/changelog-reminder-action@v2.0.0

telemetry:
  instrumentation:
    instruments:
      graphql:
        "custom_counter":
          description: "count of requests"
          type: histogram
          unit: "unit"
          value:
            field_custom:
              list_length: value


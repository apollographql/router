# yaml-language-server: $schema=/Users/tninesling/router/router.schema.json

telemetry:
  instrumentation:
    instruments:
      graphql:
        field.length:
          attributes:
            field.name: ships

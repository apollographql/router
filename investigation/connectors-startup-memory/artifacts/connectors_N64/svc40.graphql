extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.12", import: ["@key"])
  @link(url: "https://specs.apollo.dev/connect/v0.3", import: ["@connect", "@source"])
  @source(name: "svc_40", http: { baseURL: "https://svc40.example.com" })

type Query {
  svc40_item0(id: ID!): Svc40Item0
    @connect(
      source: "svc_40"
      http: { GET: "/items0/{$args.id}" }
      selection: "id name value"
    )

  svc40_item1(id: ID!): Svc40Item1
    @connect(
      source: "svc_40"
      http: { GET: "/items1/{$args.id}" }
      selection: "id name value"
    )

  svc40_item2(id: ID!): Svc40Item2
    @connect(
      source: "svc_40"
      http: { GET: "/items2/{$args.id}" }
      selection: "id name value"
    )

  svc40_item3(id: ID!): Svc40Item3
    @connect(
      source: "svc_40"
      http: { GET: "/items3/{$args.id}" }
      selection: "id name value"
    )

  svc40_item4(id: ID!): Svc40Item4
    @connect(
      source: "svc_40"
      http: { GET: "/items4/{$args.id}" }
      selection: "id name value"
    )

  svc40_item5(id: ID!): Svc40Item5
    @connect(
      source: "svc_40"
      http: { GET: "/items5/{$args.id}" }
      selection: "id name value"
    )

  svc40_item6(id: ID!): Svc40Item6
    @connect(
      source: "svc_40"
      http: { GET: "/items6/{$args.id}" }
      selection: "id name value"
    )

  svc40_item7(id: ID!): Svc40Item7
    @connect(
      source: "svc_40"
      http: { GET: "/items7/{$args.id}" }
      selection: "id name value"
    )

  svc40_shared0(id: ID!): Shared0
    @connect(
      source: "svc_40"
      http: { GET: "/shared0/{$args.id}" }
      entity: true
      selection: "id s40_a s40_b"
    )

  svc40_shared1(id: ID!): Shared1
    @connect(
      source: "svc_40"
      http: { GET: "/shared1/{$args.id}" }
      entity: true
      selection: "id s40_a s40_b"
    )

  svc40_shared2(id: ID!): Shared2
    @connect(
      source: "svc_40"
      http: { GET: "/shared2/{$args.id}" }
      entity: true
      selection: "id s40_a s40_b"
    )

  svc40_shared3(id: ID!): Shared3
    @connect(
      source: "svc_40"
      http: { GET: "/shared3/{$args.id}" }
      entity: true
      selection: "id s40_a s40_b"
    )
}

type Svc40Item0 {
  id: ID!
  name: String
  value: Int
}

type Svc40Item1 {
  id: ID!
  name: String
  value: Int
}

type Svc40Item2 {
  id: ID!
  name: String
  value: Int
}

type Svc40Item3 {
  id: ID!
  name: String
  value: Int
}

type Svc40Item4 {
  id: ID!
  name: String
  value: Int
}

type Svc40Item5 {
  id: ID!
  name: String
  value: Int
}

type Svc40Item6 {
  id: ID!
  name: String
  value: Int
}

type Svc40Item7 {
  id: ID!
  name: String
  value: Int
}

type Shared0 @key(fields: "id") {
  id: ID!
  s40_a: String
  s40_b: String
}

type Shared1 @key(fields: "id") {
  id: ID!
  s40_a: String
  s40_b: String
}

type Shared2 @key(fields: "id") {
  id: ID!
  s40_a: String
  s40_b: String
}

type Shared3 @key(fields: "id") {
  id: ID!
  s40_a: String
  s40_b: String
}

extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.12", import: ["@key"])
  @link(url: "https://specs.apollo.dev/connect/v0.3", import: ["@connect", "@source"])
  @source(name: "svc_8", http: { baseURL: "https://svc8.example.com" })

type Query {
  svc8_item0(id: ID!): Svc8Item0
    @connect(
      source: "svc_8"
      http: { GET: "/items0/{$args.id}" }
      selection: "id name value"
    )

  svc8_item1(id: ID!): Svc8Item1
    @connect(
      source: "svc_8"
      http: { GET: "/items1/{$args.id}" }
      selection: "id name value"
    )

  svc8_item2(id: ID!): Svc8Item2
    @connect(
      source: "svc_8"
      http: { GET: "/items2/{$args.id}" }
      selection: "id name value"
    )

  svc8_item3(id: ID!): Svc8Item3
    @connect(
      source: "svc_8"
      http: { GET: "/items3/{$args.id}" }
      selection: "id name value"
    )

  svc8_item4(id: ID!): Svc8Item4
    @connect(
      source: "svc_8"
      http: { GET: "/items4/{$args.id}" }
      selection: "id name value"
    )

  svc8_item5(id: ID!): Svc8Item5
    @connect(
      source: "svc_8"
      http: { GET: "/items5/{$args.id}" }
      selection: "id name value"
    )

  svc8_item6(id: ID!): Svc8Item6
    @connect(
      source: "svc_8"
      http: { GET: "/items6/{$args.id}" }
      selection: "id name value"
    )

  svc8_item7(id: ID!): Svc8Item7
    @connect(
      source: "svc_8"
      http: { GET: "/items7/{$args.id}" }
      selection: "id name value"
    )

  svc8_shared0(id: ID!): Shared0
    @connect(
      source: "svc_8"
      http: { GET: "/shared0/{$args.id}" }
      entity: true
      selection: "id s8_a s8_b"
    )

  svc8_shared1(id: ID!): Shared1
    @connect(
      source: "svc_8"
      http: { GET: "/shared1/{$args.id}" }
      entity: true
      selection: "id s8_a s8_b"
    )

  svc8_shared2(id: ID!): Shared2
    @connect(
      source: "svc_8"
      http: { GET: "/shared2/{$args.id}" }
      entity: true
      selection: "id s8_a s8_b"
    )

  svc8_shared3(id: ID!): Shared3
    @connect(
      source: "svc_8"
      http: { GET: "/shared3/{$args.id}" }
      entity: true
      selection: "id s8_a s8_b"
    )
}

type Svc8Item0 {
  id: ID!
  name: String
  value: Int
}

type Svc8Item1 {
  id: ID!
  name: String
  value: Int
}

type Svc8Item2 {
  id: ID!
  name: String
  value: Int
}

type Svc8Item3 {
  id: ID!
  name: String
  value: Int
}

type Svc8Item4 {
  id: ID!
  name: String
  value: Int
}

type Svc8Item5 {
  id: ID!
  name: String
  value: Int
}

type Svc8Item6 {
  id: ID!
  name: String
  value: Int
}

type Svc8Item7 {
  id: ID!
  name: String
  value: Int
}

type Shared0 @key(fields: "id") {
  id: ID!
  s8_a: String
  s8_b: String
}

type Shared1 @key(fields: "id") {
  id: ID!
  s8_a: String
  s8_b: String
}

type Shared2 @key(fields: "id") {
  id: ID!
  s8_a: String
  s8_b: String
}

type Shared3 @key(fields: "id") {
  id: ID!
  s8_a: String
  s8_b: String
}

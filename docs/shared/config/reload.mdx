### `reload`

```yaml title="router.yaml"
reload:
  # Maximum retry attempts after a failed reload.
  # 0 disables retries. null allows unlimited retries. Default: 5
  max_retries: 5

  # Base delay between retries. Up to 25% jitter is added automatically
  # Default: 10s
  retry_delay: 10s
```

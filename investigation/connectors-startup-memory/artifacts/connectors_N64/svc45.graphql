extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.12", import: ["@key"])
  @link(url: "https://specs.apollo.dev/connect/v0.3", import: ["@connect", "@source"])
  @source(name: "svc_45", http: { baseURL: "https://svc45.example.com" })

type Query {
  svc45_item0(id: ID!): Svc45Item0
    @connect(
      source: "svc_45"
      http: { GET: "/items0/{$args.id}" }
      selection: "id name value"
    )

  svc45_item1(id: ID!): Svc45Item1
    @connect(
      source: "svc_45"
      http: { GET: "/items1/{$args.id}" }
      selection: "id name value"
    )

  svc45_item2(id: ID!): Svc45Item2
    @connect(
      source: "svc_45"
      http: { GET: "/items2/{$args.id}" }
      selection: "id name value"
    )

  svc45_item3(id: ID!): Svc45Item3
    @connect(
      source: "svc_45"
      http: { GET: "/items3/{$args.id}" }
      selection: "id name value"
    )

  svc45_item4(id: ID!): Svc45Item4
    @connect(
      source: "svc_45"
      http: { GET: "/items4/{$args.id}" }
      selection: "id name value"
    )

  svc45_item5(id: ID!): Svc45Item5
    @connect(
      source: "svc_45"
      http: { GET: "/items5/{$args.id}" }
      selection: "id name value"
    )

  svc45_item6(id: ID!): Svc45Item6
    @connect(
      source: "svc_45"
      http: { GET: "/items6/{$args.id}" }
      selection: "id name value"
    )

  svc45_item7(id: ID!): Svc45Item7
    @connect(
      source: "svc_45"
      http: { GET: "/items7/{$args.id}" }
      selection: "id name value"
    )

  svc45_shared0(id: ID!): Shared0
    @connect(
      source: "svc_45"
      http: { GET: "/shared0/{$args.id}" }
      entity: true
      selection: "id s45_a s45_b"
    )

  svc45_shared1(id: ID!): Shared1
    @connect(
      source: "svc_45"
      http: { GET: "/shared1/{$args.id}" }
      entity: true
      selection: "id s45_a s45_b"
    )

  svc45_shared2(id: ID!): Shared2
    @connect(
      source: "svc_45"
      http: { GET: "/shared2/{$args.id}" }
      entity: true
      selection: "id s45_a s45_b"
    )

  svc45_shared3(id: ID!): Shared3
    @connect(
      source: "svc_45"
      http: { GET: "/shared3/{$args.id}" }
      entity: true
      selection: "id s45_a s45_b"
    )
}

type Svc45Item0 {
  id: ID!
  name: String
  value: Int
}

type Svc45Item1 {
  id: ID!
  name: String
  value: Int
}

type Svc45Item2 {
  id: ID!
  name: String
  value: Int
}

type Svc45Item3 {
  id: ID!
  name: String
  value: Int
}

type Svc45Item4 {
  id: ID!
  name: String
  value: Int
}

type Svc45Item5 {
  id: ID!
  name: String
  value: Int
}

type Svc45Item6 {
  id: ID!
  name: String
  value: Int
}

type Svc45Item7 {
  id: ID!
  name: String
  value: Int
}

type Shared0 @key(fields: "id") {
  id: ID!
  s45_a: String
  s45_b: String
}

type Shared1 @key(fields: "id") {
  id: ID!
  s45_a: String
  s45_b: String
}

type Shared2 @key(fields: "id") {
  id: ID!
  s45_a: String
  s45_b: String
}

type Shared3 @key(fields: "id") {
  id: ID!
  s45_a: String
  s45_b: String
}

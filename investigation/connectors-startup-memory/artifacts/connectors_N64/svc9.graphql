extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.12", import: ["@key"])
  @link(url: "https://specs.apollo.dev/connect/v0.3", import: ["@connect", "@source"])
  @source(name: "svc_9", http: { baseURL: "https://svc9.example.com" })

type Query {
  svc9_item0(id: ID!): Svc9Item0
    @connect(
      source: "svc_9"
      http: { GET: "/items0/{$args.id}" }
      selection: "id name value"
    )

  svc9_item1(id: ID!): Svc9Item1
    @connect(
      source: "svc_9"
      http: { GET: "/items1/{$args.id}" }
      selection: "id name value"
    )

  svc9_item2(id: ID!): Svc9Item2
    @connect(
      source: "svc_9"
      http: { GET: "/items2/{$args.id}" }
      selection: "id name value"
    )

  svc9_item3(id: ID!): Svc9Item3
    @connect(
      source: "svc_9"
      http: { GET: "/items3/{$args.id}" }
      selection: "id name value"
    )

  svc9_item4(id: ID!): Svc9Item4
    @connect(
      source: "svc_9"
      http: { GET: "/items4/{$args.id}" }
      selection: "id name value"
    )

  svc9_item5(id: ID!): Svc9Item5
    @connect(
      source: "svc_9"
      http: { GET: "/items5/{$args.id}" }
      selection: "id name value"
    )

  svc9_item6(id: ID!): Svc9Item6
    @connect(
      source: "svc_9"
      http: { GET: "/items6/{$args.id}" }
      selection: "id name value"
    )

  svc9_item7(id: ID!): Svc9Item7
    @connect(
      source: "svc_9"
      http: { GET: "/items7/{$args.id}" }
      selection: "id name value"
    )

  svc9_shared0(id: ID!): Shared0
    @connect(
      source: "svc_9"
      http: { GET: "/shared0/{$args.id}" }
      entity: true
      selection: "id s9_a s9_b"
    )

  svc9_shared1(id: ID!): Shared1
    @connect(
      source: "svc_9"
      http: { GET: "/shared1/{$args.id}" }
      entity: true
      selection: "id s9_a s9_b"
    )

  svc9_shared2(id: ID!): Shared2
    @connect(
      source: "svc_9"
      http: { GET: "/shared2/{$args.id}" }
      entity: true
      selection: "id s9_a s9_b"
    )

  svc9_shared3(id: ID!): Shared3
    @connect(
      source: "svc_9"
      http: { GET: "/shared3/{$args.id}" }
      entity: true
      selection: "id s9_a s9_b"
    )
}

type Svc9Item0 {
  id: ID!
  name: String
  value: Int
}

type Svc9Item1 {
  id: ID!
  name: String
  value: Int
}

type Svc9Item2 {
  id: ID!
  name: String
  value: Int
}

type Svc9Item3 {
  id: ID!
  name: String
  value: Int
}

type Svc9Item4 {
  id: ID!
  name: String
  value: Int
}

type Svc9Item5 {
  id: ID!
  name: String
  value: Int
}

type Svc9Item6 {
  id: ID!
  name: String
  value: Int
}

type Svc9Item7 {
  id: ID!
  name: String
  value: Int
}

type Shared0 @key(fields: "id") {
  id: ID!
  s9_a: String
  s9_b: String
}

type Shared1 @key(fields: "id") {
  id: ID!
  s9_a: String
  s9_b: String
}

type Shared2 @key(fields: "id") {
  id: ID!
  s9_a: String
  s9_b: String
}

type Shared3 @key(fields: "id") {
  id: ID!
  s9_a: String
  s9_b: String
}

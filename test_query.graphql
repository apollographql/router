query ExampleQuery {
  topProducts(first: 10) {
    name
    price
    upc
    inStock
  }
  me {
    id
    reviews {
      id
      body
      product {
        upc
        name
        inStock
        price
      }
    }
  }
}
